// Serviço de IA removido conforme solicitação.
// Este arquivo agora serve apenas como placeholder caso queira reativar funcionalidades futuras.

export const generateResponse = async (): Promise<string> => {
  return "";
};
